spring:
  config.activate.on-profile: default
  sql.init.schema-locations: classpath:schema-h2.sql
  config:
    activate:
     on-profile: cloud
cds:
  index-page:
    enabled: true
  datasource:
    audit.config.enable: false

  security:
    mock:
      users:
        - name: max
          password: pass@123
          additional:
            lastName: Kumar
            firstName: Max
            email: max.kumar@test.com

        - name: emma
          password: pass@456
          roles:
           'Admin'
          additional:
            lastName: Brown
            firstName: Emma
            email: emma.brown@edu.com
---
management:
  endpoint:
    health:
      show-components: always
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
  health:
    defaults:
      enabled: false
    ping:
      enabled: true
    db:
      enabled: true
